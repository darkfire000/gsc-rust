sudo: required
services:
  - docker
script:
  - docker build . -t egeeio/rustserver
after_success:
  - docker login -u $username -p $password
  - docker push egeeio/rustserver
  - echo "Image Update & Upload Complete"
